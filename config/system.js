const PATH_ADMIN = "/admin"; // Khai báo một hằng số 'PATH_ADMIN' với giá trị là đường dẫn "/admin"

module.exports = {
  // Xuất khẩu một đối tượng cấu hình
  prefixAdmin: PATH_ADMIN, // Gán giá trị của 'PATH_ADMIN' cho thuộc tính 'prefixAdmin' của đối tượng cấu hình
};
